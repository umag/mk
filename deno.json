{
  "unstable": ["sloppy-imports"],
  "tasks": {
    "server": "deno run --allow-net --allow-read --allow-write --allow-env --allow-ffi server/main.ts"
  },
  "fmt": {
    "exclude": ["node_modules", "dist", ".impeccable"]
  },
  "lint": {
    "exclude": ["node_modules", "dist", ".impeccable"]
  }
}
